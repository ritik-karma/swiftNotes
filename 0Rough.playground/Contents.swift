

func maxProfit(_ prices: [Int]) -> Int {
    if prices.isEmpty { return 0}
    var buyIndex = 0
    var sellIndex = 1
    var maxProfit = 0
    
    while (sellIndex < prices.count ) {
          let buy = [buyIndex]
        let sell = [sellIndex]
        
        
        
    }
        
    return 0
}


var x = [7,6,4,3,1]
x.max()
maxProfit(x)
